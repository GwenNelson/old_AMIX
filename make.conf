FILENAME:=$(lastword $(MAKEFILE_LIST))

SRCROOT:=$(realpath $(dir $(FILENAME)))

SYSROOT:=$(SRCROOT)/sysroot

ARCH?=i386

CFLAGS?=-O0 -gstabs3  -fno-eliminate-unused-debug-types 
NASMFLAGS?=-g -F stabs

CC=$(ARCH)-elf-gcc --sysroot=$(SYSROOT) -isystem=/usr/include
AS=$(ARCH)-elf-as
LD=$(ARCH)-elf-ld
AR=$(ARCH)-elf-ar
